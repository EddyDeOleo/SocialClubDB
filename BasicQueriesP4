
-- Persona 1
SELECT * FROM Socios;

SELECT categoria_id, nombre_categoria, descripcion, lista_privilegios, lista_beneficios
FROM CategoriasMembresia;

-- Persona 2
SELECT usuario_id, nombre_usuario, rol_usuario, estado_usuario, socio_id
FROM UsuariosSistema;

SELECT * FROM Membresias;

-- Persona 3
SELECT pago_id, socio_id, fecha_pago, monto_pago, estado_pago
FROM Pagos;

SELECT * FROM Facturas;

-- Persona 4
SELECT evento_id, nombre_evento, tipo_evento, fecha_evento, lugar_evento
FROM Eventos;

SELECT * FROM Inscripciones;

-- Persona 5
SELECT instalacion_id, nombre_instalacion, tipo_instalacion, disponibilidad
FROM Instalaciones;

SELECT * FROM Reservas;
